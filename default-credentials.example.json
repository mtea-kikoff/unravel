{
  "_comment": "Copy this file to default-credentials.json and fill in the shared OAuth client. That file is gitignored and gets bundled into the packaged app so coworkers connect without any Google Cloud setup. See README (Distributing to coworkers).",
  "clientId": "REPLACE_WITH_CLIENT_ID.apps.googleusercontent.com",
  "clientSecret": "REPLACE_WITH_CLIENT_SECRET"
}
